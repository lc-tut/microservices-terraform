# cp .env.example .env して値を入れる（.env は gitignore 済み）
MARIADB_ROOT_PASSWORD=change-me-root
CK_DB_PASSWORD=change-me-ck
# Polaris(Kolla) の admin パスワード（lc-sv01:/etc/kolla/admin-openrc.sh 参照）
OS_ADMIN_PASSWORD=change-me-os-admin

# イメージタグ（再現性のため固定）
KOLLA_TAG=2026.1-rocky-10
OS_EXPORTER_TAG=2.0.0-alpha
PROMETHEUS_TAG=v3.1.0
